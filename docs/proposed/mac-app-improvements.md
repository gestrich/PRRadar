We don't need seprate views on the side for diff, focus areas, rules, tasks, evaltuaions, report.

Instead... We shoudl have a view with


[ File Selection list + a summary row for all file summary (what is in our current summary view)] 

* Tapping a file in list would show Show the diff for that file. 
* A buton would be available You can toggle to effectie diff
* Th elist of tasks (rules) for that file woudl show underneath. You can expand it to see the rule descrption.
* A play button woudl be next to each task which measn to ealute that specific task.  
* After it runs a checkmark woudl show meaning it was run
* IF there are comment to add, they woudl show inline in the diff with a button to send that comment to the PR.
* The style of the comment in the diff view woudl look like Github style.
* THe sumamry view for all files would